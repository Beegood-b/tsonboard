"use strict";
let myName = 'Artjoms';
let meaningOfLife;
let isLoading;
let album; // union type
myName = 'John';
meaningOfLife = 42;
isLoading = true;
album = 1881;
const sum = (a, b) => {
    return a + b;
};
let postId;
let isActive; // union types are not limited only to two data types
let re = /\w+/g; // regular expression type
